# OKbroGATE Design Brief

> **용도:** 디자인 전용 대화에 넘기는 완결 브리프  
> **기준일:** 2026-07-14  
> **근거:** `src/` 코드 스캔 + 최근 기획 논의(로그인/GPS/PWA/알람/목록 분기)  
> **주의:** 요청문에 1단계 요약 붙여넣기가 비어 있어(`[여기에 … 붙여넣기]`), 이 문서는 **실제 코드 상태**를 기준으로 하고, 논의에서 나온 방향·제약은 별도 섹션으로 구분함.

---

## 0. 제품 한 줄

마라톤/그란폰도 **스포츠 사진 게이트** 웹앱(PWA).  
예정 대회에서 GPS로 촬영 지점 통과를 감지하고, 구매 인증·GPS로 고화소(B) 앨범에 접근하게 한다. 과일 스토어 CTA가 상시 노출된다.

---

## 1. 기획 맥락 ↔ 코드 구현 상태

### 1.1 이미 코드에 있는 것 (선수/어드민 UI 기준)

| 영역 | 현재 UI에 존재 |
|------|----------------|
| 브랜드·라이트 테마 | Primary `#FF5500`, Secondary `#1F2937`, 밝은 배경 |
| PWA | `manifest.json`, 홈 **앱 설치하기** CTA + 설치 가이드 모달 |
| 홈 히어로 | 배경 이미지(어드민 설정) + 어두운 오버레이 + 가이드 카피 |
| 출사 예정 / 출사(과거) 목록 | 날짜·B앨범 유무로 분기된 두 섹션 |
| 예정 대회 GPS 토글 | 카드 옆 ON/OFF (리스트용 파란 스위치 변형) |
| 대회 상세 GPS | 촬영 감지 토글, 체크리스트, 추적 중 배너·거리 표시 |
| 앨범 A / 구매 유도 / 앨범 B | 인증 분기에 따른 뷰·모달 |
| 마이페이지 | 열람 만기 D-day, 인증 연장, **촬영 감지 이력**, **촬영 알림 켜기** |
| 공지 | 홈 배너(최대 높이 ~50vh, 주황 악센트) + `/notification` |
| 온보딩 | 첫 실행 권한·약관·검증 유도 모달/게이트 |
| 하단 과일 CTA | 전역(어드민 제외) |
| 어드민 | 대회/설정/선수/대회별 모니터링, GPS 지도, 공지·배경 관리 |
| 대회별 모니터링 | GPS **감지 ON** / 통과 여부 등 (로그 없어도 감지 ON이면 리스트 포함) |

### 1.2 논의됐으나 UI/제품적으로 열린 것 · 미완성으로 취급할 것

| 주제 | 상태 | 디자인 함의 |
|------|------|-------------|
| **다크 에디토리얼 / 힙 MZ 전면 리디자인** | 코드·문서에 **미반영**. 현재는 **라이트 + 스포츠 유틸리티** 톤 | 새 비주얼 방향이면 **전면 재설계**로 취급 |
| **폰 푸시 “N시에 촬영했어요” 일상 알림** | 서버 발송 경로·마이페이지 알림 켜기는 있음. 런타임 체감·일관 UX는 불완전할 수 있음 | 디자인 시 “푸시 카피/시점”과 “앱 내 토스트만”을 **분리해서** 설계 |
| **지오펜싱 조기 알림** (`오켕이 기다리고 있어요`, 반경×3) | 코드에 **있음**. 기획상 “달리는 중엔 불필요” 의견 있음 | 유지/삭제/약화 결정 필요 |
| **통과 직후 인앱 토스트** (`촬영 지점 도착! 시각`) | 있음. 달릴 때보다 **종료 후 확인**용으로 인식 | 시각·히스토리 UI와 역할 정리 |
| **백그라운드 GPS** | 웹 한계로 **불가**. 배너/체크리스트에 “앱·상세 유지” 안내 | 오해 없게 카피 유지 |
| **HTTP vs HTTPS GPS** | 코드 강제 없음. 프로덕션 HTTPS + localhost 예외 | 디자인과 무관, 테스트 환경 제약만 |

### 1.3 구현됐지만 디자인 시 건드리지 말 것(기능 제약)

- GPS 토글·추적 배너·체크리스트는 **눈에 잘 띄어야 함** (가독성·대비 필수).
- 홈 **앱 설치** CTA는 주요 진입 액션으로 유지.
- 하단 **과일 구매 CTA** 영역 예약(하단 패딩) 유지.
- 어드민은 선수 크롬(네비/과일 CTA) **없음** — 콘솔형으로 분리 가능.
- 목록 GPS 토글만으로는 추적 안 됨 → **대회 상세**에서만 실제 감지. 카피/플로우에 반영.

---

## 2. 화면 목록과 현재 UI 요소

### 2.1 선수

| 경로 | 역할 | 현재 UI 요소 |
|------|------|----------------|
| `/` | 홈 | 히어로(50vh)·가이드 카피, **앱 설치** sticky CTA, 만기 배너, 공지 배너, **오켕 출사 예정**, **오켕 출사**(과거) |
| `/login` | 로그인 | 카드, 아이콘, Google 로그인, 인앱 브라우저 경고/외부열기 |
| `/signup` | 회원가입 | 로그인과 유사 카드 |
| `/mypage` | 마이페이지 | 만기 D-day 카드, 촬영 감지 이력, 촬영 알림, 인증 연장 폼 |
| `/events/[id]` | 대회 상세 | 약관 게이트, GPS 추적 배너, 촬영 감지 카드, A/중간/B 앨범 분기 |
| `/verify-order` | 구매 인증 | 주문번호 폼, 결과, 앨범 연결 (일부 인라인 스타일) |
| `/notification` | 공지 전체 | 주황 보더 공지 카드 |
| `/photos/[id]` | (레거시) 단일 사진 | 블러 프리뷰·인증·다운로드 — **톤이 본편과 다름**(어두운 카드 잔존) |

전역 선수 크롬 (`PlayerAppChrome`, `/admin` 제외):

- 상단 `SiteNav` (높이 56px) — `OKbro`GATE / 대회 목록 / 마이·로그인
- 하단 `FixedFruitCta`
- 첫 실행 권한·인앱 브라우저 차단 등

### 2.2 어드민 (`/admin`)

탭: 대회 관리 · 설정 · 선수 관리 · 대회별 모니터링 (+ 공지·홈배경 등 패널)

- 테이블·폼 중심, Leaflet GPS 좌표 지도
- 대회별 모니터링: 선수명, **GPS 감지**, GPS 통과, 시각, pass_count, notified

### 2.3 개발 전용

- `/dev/gps-hint-preview`, `/dev/album-flow-preview` — 디자인 프로덕션 범위 밖

---

## 3. 현재 디자인 시스템 (코드 실측)

**소스:** `src/app/globals.css` (Tailwind v4 `@theme` + `:root`)  
별도 `tailwind.config` 색상 파일 없음.

### 3.1 색

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--color-okbro-orange` / `--primary` | `#FF5500` / `#ff5500` | CTA, 강조, themeColor, 공지·GPS 강조 |
| `--primary-hover` | `#e04a00` | 호버 |
| `--color-okbro-dark` / `--secondary` / `--text` | `#1F2937` | 본문·Secondary |
| `--color-okbro-green` / `--success` | `#10B981` | 성공 |
| `--bg` | `#F3F4F6` | 페이지 배경 |
| `--bg-card` / surface | `#FFFFFF` | 카드 |
| `--text-muted` | `#6B7280` | 보조 텍스트 |
| `--border` | `#E5E7EB` | 보더 |
| `--accent-dim` / warning-bg | `#FFF4ED` | 연한 주황 틴트 |
| danger | `#EF4444` | 오류·임박 |

**예외:** 예정 목록 GPS 토글 ON은 **파란** `#2563EB` 계열(`.toggle-switch-events-on`) — 상세 주황 토글과 이중 체계.

### 3.2 타이포

- `system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif`
- body ~15px, line-height 1.6
- `.page-title` ~24→32px bold, `.section-title` ~16→18px
- **커스텀 디스플레이 폰트 없음** (Express / editorial 폰트 미도입)
- 카피에 **이모지** 다수 사용

### 3.3 레이아웃·형태

- 선수 컨테이너: `max-w-lg` 중심 모바일
- 카드: `rounded-xl` ~ `rounded-2xl`, 약한 그림자
- 하단 CTA 예약: `padding-bottom: calc(84px + safe-area)`
- 히어로만 사진 위 **다크 그라데이션 오버레이** — 앱 전역 다크모드 아님

### 3.4 현재 톤 (한 줄)

**밝은 배경 + 주황 스포츠 게이트 유틸리티.**  
패션/에디토리얼 다크, MZ 스트리트 룩은 **아직 없음**.

---

## 4. 디자인 방향성 (논의) vs 기능 제약

### 4.1 방향성 (브리프에 붙인 예시 / 미구현)

요청에서 언급된 예시:

- 다크 에디토리얼
- 힙한 MZ 감성

**코드 기준:** 위 방향은 **목표 가설**일 뿐, 현행 UI와 **불연속**.  
리디자인 시 결정해야 할 것:

1. 브랜드 주황 `#FF5500`을 **시그니처로 유지**할지, 톤만 다크로 옮길지  
2. 시스템 폰트 유지 vs 디스플레이/본문 폰트 도입  
3. 이모지 과다 vs 아이콘 체계화  
4. 히어로 full-bleed 사진 언어를 **전 앱으로 확장**할지  

### 4.2 기능 제약 (디자인이 깨면 안 되는 것)

| 제약 | 이유 |
|------|------|
| **GPS 토글 고가시성** | 경기 전 ON이 핵심 행동 |
| **추적 중 배너 + “앱/상세 유지” 안내** | 웹 GPS는 백그라운드 불가 |
| **홈 PWA 설치 CTA** | 현장·알림 경험의 진입점 |
| **하단 과일 CTA** | 비즈니스 CTA, 레이아웃 하단 점유 |
| **예정 vs 과거 정보 구조** | `album_b`·날짜 기반 분기 — 카피/섹션 제목과 맞출 것 |
| **목록 토글 ≠ 추적 시작** | 상세 유도 UX 필요 |
| **어드민 밀도** | 운영 콘솔 — 선수 비주얼과 분리해도 됨 |
| **HTTPS(실기기)** | 일반 HTTP IP로 폰 테스트 시 GPS 실패 가능 — “버그처럼 보이는” UX |

### 4.3 알람 UX (디자인 시 명확히 나눌 것)

| 종류 | 현재 | 선수 상황 |
|------|------|-----------|
| 앱 내 토스트(근처/도착) | 상세+추적 중일 때만 | 달릴 때 확인 어려움 → 조기 알림은 우선순위↓ 논의 |
| 마이페이지 감지 이력 | 구현됨 | 사후 확인용으로 중요 |
| OS 푸시 촬영시각 | 부분/불완전 가능 | “창 닫혀도 푸시”는 가능 기술이지만 제품 완성도와 별개 |

---

## 5. 정보 구조 요약 (디자인 IA)

```
홈
 ├ 설치 CTA / 공지 / 만기
 ├ 출사 예정 (GPS 토글, 위치 미리보기)
 └ 출사 과거 (통과 시각 문구, 앨범 진입)

대회 상세
 ├ 약관
 ├ GPS 감지·추적 UI  ★핵심
 └ 앨범 A | 구매 유도 | 앨범 B

마이페이지
 ├ 열람 만기
 ├ 촬영 감지 이력
 ├ 촬영 알림
 └ 인증 연장

어드민 (별도 톤 가능)
```

---

## 6. 디자인 작업 권장 범위 (다음 대화용)

**In scope**

- 선수 표면: 홈·상세·마이·로그인·공지·인증·앨범 흐름의 **비주얼·타이포·컴포넌트 통일**
- GPS/설치/과일 CTA의 **가시성·위계**를 살린 리디자인
- (선택) 다크 에디토리얼·MZ 톤으로의 **의도적 점프** — 현 라이트 시스템을 대체하는 제안

**Out of scope (이 브리프에서 의도적으로 제외)**

- API·스키마·버그픽스 이력
- 어드민 테이블 UX의새 (원하면 2차)
- 레거시 `/photos/[id]` 우선순위는 낮음(정리 후보)

---

## 7. 참고 파일

| 종류 | 경로 |
|------|------|
| 토큰·컴포넌트 스타일 | `src/app/globals.css` |
| 루트 레이아웃·PWA 메타 | `src/app/layout.tsx` |
| 홈 | `src/components/landing-page.tsx` |
| 네비·과일 CTA | `src/components/site-nav.tsx`, `fixed-fruit-cta.tsx` |
| GPS | `src/components/gps-detector.tsx`, `gps-tracking-toggle.tsx`, `gps-tracking-banner.tsx` |
| PWA 설치 | `landing-page.tsx` + `pwa-install-guide-modal.tsx`, `public/manifest.json` |
| 선수 크롬 | `src/components/player-app-chrome.tsx` |

---

## 8. 원문 붙여넣기 메모

요청에 **1단계 요약 본문이 포함되지 않음**.  
나중에 기획 요약을 받으면, §1의 “미구현/방향성” 표를 그에 맞춰 갱신하면 된다.

**현행 진실:** 라이트·주황 스포츠 게이트 UI가 프로덕션 코드의 실체이고,  
다크 에디토리얼/MZ는 **다음 디자인의 가설**로만 다룬다.
